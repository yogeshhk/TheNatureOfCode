# TODO — Outstanding Manual Follow-ups

Everything from the `/upgrade-repo-tech` review plan (approved 2026-07-14) that
an agent could do is done — see `reports/upgrade_14072026.md` for the full
write-up. What's left are items that need you, not an agent:

- [ ] **Screenshots/GIFs of example sketches.** py5 opens a real GUI window,
      which this environment can't render headlessly. Run a few visually
      interesting sketches locally (e.g. `noc_2_6_gravitationalattraction.py`,
      `noc_3_5_b_springs.py`, `noc_3_4_b_pendulumsimulation.py`), capture a
      screenshot or short GIF of each, and add them to the README.
- [ ] **GitHub repo Settings — Description + Topics.** Needs the GitHub web UI:
      1. Go to `https://github.com/yogeshhk/TheNatureOfCode`.
      2. Click the gear icon next to "About" in the right sidebar.
      3. Set **Description**, e.g. "Python (py5) implementations of Daniel
         Shiffman's The Nature of Code."
      4. Add **Topics**: e.g. `py5`, `processing`, `nature-of-code`,
         `physics-simulation`, `python`, `generative-art`.
      5. Optionally set a **social preview image** once a screenshot exists.
      6. Click "Save changes."
- [ ] **LinkedIn post** (draft + banners already prepared in `docs/`; do not
      publish without explicit go-ahead):
      - [ ] Review/edit `docs/linkedin_post_draft.md` and the banner SVGs.
      - [ ] Export the chosen SVG (`docs/banner_wide.svg` or
            `docs/banner_square.svg`) to PNG — LinkedIn doesn't accept SVG.
      - [ ] Publish when you're ready — manual step, not automated.

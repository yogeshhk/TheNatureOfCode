# File Guide - The Nature of Code (Python)

> [!WARNING]
> **Disclaimer**: This documentation was generated with the assistance of Gemini 2.0 Pro AI. While we strive for accuracy, there is a chance of incorrect information or hallucinations. Please verify details with the source code.

This guide details the purpose, concepts, and implementation details of each example in the `src` directory.

| File | Concept & Purpose | Key P5 & Code Features | Run Command |
| :--- | :--- | :--- | :--- |
| [`src/noc-installation-test.py`](src/noc-installation-test.py) | **Installation Check**: A simple drawing script to verify that `p5` and `GLFW` are correctly installed and a window can be opened. | Basic shapes: `rect()`, `ellipse()`, `line()`. | `.venv\Scripts\python src/noc-installation-test.py` |
| [`src/noc_1_1_vectors.py`](src/noc_1_1_vectors.py) | **Bouncing Ball (No Vectors)**: Demonstrates motion *without* using vectors to highlight the complexity of managing separate scalar variables (`x`, `y`, `xspeed`, `yspeed`) for motion. | Manual position updates: `x = x + xspeed`. Basic rendering: `ellipse()`, `background()`. | `.venv\Scripts\python src/noc_1_1_vectors.py` |
| [`src/noc_1_2_pvectorclass.py`](src/noc_1_2_pvectorclass.py) | **Bouncing Ball (Vectors)**: Refactors the previous example to use a `Vector` class (or PVector). Demonstrates how vectors simplify state management by bundling position and velocity. | `Vector` class usage. Vector addition: `self.location += self.velocity`. | `.venv\Scripts\python src/noc_1_2_pvectorclass.py` |
| [`src/noc_1_3_vectormath.py`](src/noc_1_3_vectormath.py) | **Vector Math Basics**: Visualizes vector subtraction. Draws a line from center to mouse to demonstrate the concept of a vector representing a difference in position. | `Vector.sub(mouse, center)` to get a vector pointing to the mouse. `translate()` to center origin. | `.venv\Scripts\python src/noc_1_3_vectormath.py` |
| [`src/noc_1_4_vectormathII.py`](src/noc_1_4_vectormathII.py) | **Vector Math Operations**: Demonstrates vector multiplication (scaling). Visualizes a vector to the mouse that grows/shrinks (scales) based on a multiplier. | `Vector.mult()` to scale magnitude. `translate()` for coordinate systems. | `.venv\Scripts\python src/noc_1_4_vectormathII.py` |
| [`src/noc_1_5_acceleation.py`](src/noc_1_5_acceleation.py) | **Constant Acceleration**: Introduces the concept of acceleration as the rate of change of velocity. The object speeds up in a random direction indefinitely. | `Vector.random_2D()` for random unit vectors. `velocity.add(acceleration)`. | `.venv\Scripts\python src/noc_1_5_acceleation.py` |
| [`src/noc_1_6_acceleationtowardsmouse.py`](src/noc_1_6_acceleationtowardsmouse.py) | **Interactive Acceleration**: Simulates attraction. Calculates a vector pointing from the object *to* the mouse, normalizes it, and applies it as acceleration. | `Vector.sub()` for direction. `normalize()` and `mult()` to set force magnitude. | `.venv\Scripts\python src/noc_1_6_acceleationtowardsmouse.py` |
| [`src/noc_2_1_whatisaforce.py`](src/noc_2_1_whatisaforce.py) | **Force Accumulation**: efficient implementation of Newton's 2nd Law ($F=MA$). Shows how to accumulate multiple forces (like wind, gravity) into acceleration. | `applyForce()` method implementation. Vector addition for accumulating forces. | `.venv\Scripts\python src/noc_2_1_whatisaforce.py` |
| [`src/noc_2_2_applyingaforce.py`](src/noc_2_2_applyingaforce.py) | **Applied Forces**: Simulates an object affected by Wind (right push) and Gravity (down push). | Creation of arbitrary force Vectors (e.g. `Vector(0, 0.1)`). | `.venv\Scripts\python src/noc_2_2_applyingaforce.py` |
| [`src/noc_2_3_simulatingwithmass.py`](src/noc_2_3_simulatingwithmass.py) | **Mass Effect**: Demonstrates that lighter objects accelerate faster for the same force. Implements $A = F / M$. | `Vector.div()` to divide force by mass. Generating objects with random `mass`. | `.venv\Scripts\python src/noc_2_3_simulatingwithmass.py` |
| [`src/noc_2_4_frictionforce.py`](src/noc_2_4_frictionforce.py) | **Friction**: Simulates surface friction which opposes motion. Calculates friction vector based on inverse velocity and coefficient of friction. | `velocity.copy()`, `normalize()`, `mult(-1)` to reverse direction. | `.venv\Scripts\python src/noc_2_4_frictionforce.py` |
| [`src/noc_2_5_a_dragforce.py`](src/noc_2_5_a_dragforce.py) | **Fluid Drag**: Simulates an object falling through a liquid. Drag force increases with speed squared ($v^2$). | `velocity.mag()` to get speed. Calculating drag magnitude and applying opposite loop. | `.venv\Scripts\python src/noc_2_5_a_dragforce.py` |
| [`src/noc_2_5_b_dragforce.py`](src/noc_2_5_b_dragforce.py) | **Drag (OO Architecture)**: Encapsulates the drag logic into a `Liquid` class. Checks if `Mover` is inside `Liquid` to apply drag. | Class interaction: `liquid.contains(mover)` and `liquid.drag(mover)`. | `.venv\Scripts\python src/noc_2_5_b_dragforce.py` |
| [`src/noc_2_6_gravitationalattraction.py`](src/noc_2_6_gravitationalattraction.py) | **Gravitational Attraction**: Simulates gravity between two objects using Newton's Universal Law of Gravitation ($F = (G*m1*m2)/dist^2$). | `Vector.dist()` calculating distance. `constrain()` to prevent extreme forces at close range. | `.venv\Scripts\python src/noc_2_6_gravitationalattraction.py` |
| [`src/noc_3_1_a_anglesandangularmotion.py`](src/noc_3_1_a_anglesandangularmotion.py) | **Angular Motion**: Rotating a graphical object (baton) using `rotate()`. Demonstrates separating rotation from translation. | `push_matrix()`, `translate()`, `rotate()`, `pop_matrix()` stack. | `.venv\Scripts\python src/noc_3_1_a_anglesandangularmotion.py` |
| [`src/noc_3_1_b_anglesandangularmotion.py`](src/noc_3_1_b_anglesandangularmotion.py) | **Angular Velocity**: Adds physics to rotation. Angular acceleration updates angular velocity, which updates angle. | Variable `aVelocity`, `aAcceleration`. `constrain()` for stability. | `.venv\Scripts\python src/noc_3_1_b_anglesandangularmotion.py` |
| [`src/noc_3_2_trigonometryandpolarcoordinates.py`](src/noc_3_2_trigonometryandpolarcoordinates.py) | **Polar Coordinates**: Demonstrates positioning objects using Angle and Radius ($r, \theta$) instead of $X, Y$. | Trigonometry: `x = r * cos(theta)`, `y = r * sin(theta)`. | `.venv\Scripts\python src/noc_3_2_trigonometryandpolarcoordinates.py` |
| [`src/noc_3_3_simpleharmonicmotion.py`](src/noc_3_3_simpleharmonicmotion.py) | **Simple Harmonic Motion**: Uses the Sine wave property to create smooth oscillation (swinging back and forth). | `sin()` function mapped to x-position. using `frame_count` or angle as input. | `.venv\Scripts\python src/noc_3_3_simpleharmonicmotion.py` |
| [`src/noc_3_4_a_pendulumsimulation.py`](src/noc_3_4_a_pendulumsimulation.py) | **Pendulum Physics**: procedural pendulum. Calculates angular acceleration based on gravity's component acting on the arm length. | Physics formula: `aAcc = -1 * gravity * sin(angle)`. | `.venv\Scripts\python src/noc_3_4_a_pendulumsimulation.py` |
| [`src/noc_3_4_b_pendulumsimulation.py`](src/noc_3_4_b_pendulumsimulation.py) | **Pendulum Class**: Encapsulates pendulum logic into a class. Adds mouse interaction to drag the bob. | `atan2()` to calculate angle between mouse and pivot point during interaction. | `.venv\Scripts\python src/noc_3_4_b_pendulumsimulation.py` |
| [`src/noc_3_5_a_springs.py`](src/noc_3_5_a_springs.py) | **Spring Force**: Hooke's Law simulation ($F = -k * x$). Spring force seeks target length. | Vector math to find extension/compression. `normalize()` and `mult()` for spring force. | `.venv\Scripts\python src/noc_3_5_a_springs.py` |
| [`src/noc_3_5_b_springs.py`](src/noc_3_5_b_springs.py) | **Connecting Springs**: Multiple objects connected by springs. Architecture for spring systems. | `Spring` and `Bob` classes interacting. | `.venv\Scripts\python src/noc_3_5_b_springs.py` |
| [`src/noc_I_0_introduction.py`](src/noc_I_0_introduction.py) | **Template**: Empty introductory file. | N/A | `.venv\Scripts\python src/noc_I_0_introduction.py` |
| [`src/noc_I_1_randomwalker.py`](src/noc_I_1_randomwalker.py) | **Random Walker**: The core concept of randomness. A point that moves in a random direction each frame. | `random(4)` to pick between Up, Down, Left, Right. `constrain()` to keep in screen. | `.venv\Scripts\python src/noc_I_1_randomwalker.py` |
| [`src/noc_I_2_probabilitybasics.py`](src/noc_I_2_probabilitybasics.py) | **Non-Uniform Probability**: A walker that tends to move right. Demonstrates checking random values against probability thresholds (< 0.4 etc). | `if random() < 0.4:` logic for weighted choices. | `.venv\Scripts\python src/noc_I_2_probabilitybasics.py` |
| [`src/noc_I_3_a_gaussiandistrbution.py`](src/noc_I_3_a_gaussiandistrbution.py) | **Gaussian Distribution**: Drawing circles at positions determined by a normal (bell curve) distribution, clustering in the center. | `random_gaussian()` usage for natural "clustering" randomness. | `.venv\Scripts\python src/noc_I_3_a_gaussiandistrbution.py` |
| [`src/noc_I_3_b_gaussiandistrbution.py`](src/noc_I_3_b_gaussiandistrbution.py) | **Gaussian Visualization**: Paints dots with color transparency to visualize the density of the normal distribution. | `no_stroke()`, `fill()` with alpha channel. | `.venv\Scripts\python src/noc_I_3_b_gaussiandistrbution.py` |
| [`src/noc_I_4_customdistrbution.py`](src/noc_I_4_customdistrbution.py) | **Custom Distribution**: Implementing an algorithm (Monte Carlo) to generate random numbers with a custom "bias" (e.g. higher numbers are more likely). | Implementation of "Accept-Reject" algorithm loop. | `.venv\Scripts\python src/noc_I_4_customdistrbution.py` |
| [`src/noc_I_5_perlinnoise.py`](src/noc_I_5_perlinnoise.py) | **Perlin Noise**: Demonstrates "smooth" randomness. Uses `noise()` to map x/y position, creating organic movement instead of jittery random. | `noise(t)` function. `map()` to convert noise (0-1) to screen coordinates. | `.venv\Scripts\python src/noc_I_5_perlinnoise.py` |
| [`src/pyp5-bezier-test.py`](src/pyp5-bezier-test.py) | **Bezier Test**: A utility script to specifically test the drawing of Bezier curves. | `bezier()` function usage. | `.venv\Scripts\python src/pyp5-bezier-test.py` |

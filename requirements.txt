# py5 (https://py5coding.org/) pulls in its own platform-appropriate
# dependencies automatically (numpy, pillow, pywin32 on Windows,
# pyobjc on macOS, etc.) — no need to list them separately here.
# Requires Python 3.10+ and Java 17+. See SETUP_AND_RUN.md.
py5
